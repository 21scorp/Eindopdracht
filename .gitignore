node_modules/
dist/
.DS_Store
*.local
.vite/
coverage/
*.log

# Regenerated by tools/smoke.mjs, tools/viewports.mjs and tools/capture.mjs.
# These are review artefacts, not sources — the curated set the README uses
# lives in docs/media/ and is written at 1x by `node tools/capture.mjs --media`.
tools/shots/
